# Secrets — never commit
.env
*.env

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/
*.egg-info/
dist/
build/

# Drop folder contents (but keep the folder itself)
drop_folder/*
!drop_folder/.gitkeep

# WhatsApp session
whatsapp_session/

# OS
.DS_Store
Thumbs.db

# Logs (optional — remove if you want to track logs in git)
AI_Employee_Vault/Logs/*.log

# Node (for future MCP servers)
node_modules/
